<!-- using core module fs and path

## Steps to Run `index.js` in the `readablestream` Folder

1. Open your terminal or command prompt.
2. Navigate to the `readablestream` directory:
    ```bash
    cd /c:/nodejs-basic/readablestream
    ```
3. Make sure you have Node.js installed. You can check by running:
    ```bash
    node -v
    ```
4. Run the `index.js` file using Node.js:
    ```bash
    node index.js
    ```
5. Observe the output in your terminal. -->